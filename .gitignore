# Python
__pycache__/
*.pyc
*.pyo

# local state / scratch
*.db
*.db-wal
*.db-shm
*.bak
*.keys
*.he

# secrets / local env (never publish)
.env
*.env

# local deployment config / demos that carry LAN infra details (never publish)
config.yaml
demo_resilient_business_memory.py

# test-run noise (the .md summaries + .jsonl metrics ARE committed; raw logs are not)
results/*.log

# local RLM<->grok connection config (LAN host/key stays off GitHub)
rlm-grok.conf
